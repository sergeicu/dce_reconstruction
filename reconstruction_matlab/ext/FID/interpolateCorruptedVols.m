function [ interpData ] = interpolateCorruptedVols( data, corruptedVols, startInterp )
    
    if ~exist('startInterp')
       startInterp = 2 
    end

    T = numel(corruptedVols);
    
    for tt = startInterp:T-1
        
        
    end

        
end

